"""College football (FBS) pipeline: the NFL model's data adapter, features,
training, props and page export, built on the free sportsdataverse releases
and ESPN's public endpoints. Nothing here needs an API key."""
